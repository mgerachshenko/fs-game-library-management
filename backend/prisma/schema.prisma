generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model UserProfile {
  id          Int     @id @default(autoincrement())
  displayName String
  bio         String?
  avatar      String?
}