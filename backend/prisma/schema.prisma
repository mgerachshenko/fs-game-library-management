generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model UserProfile {
  id          Int     @id @default(autoincrement())
  displayName String
  bio         String?
  avatar      String?
}